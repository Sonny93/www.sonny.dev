name: app-prod
services:
  app:
    restart: always
    build:
      context: .
    environment:
      - HOST=0.0.0.0
      - NODE_ENV=production
    env_file:
      - .env
    ports:
      - ${PORT}:3333

volumes:
  redis-data:
